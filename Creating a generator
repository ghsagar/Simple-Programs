# constructing generator
# Task----> define a function that takes one number as an argument and return every even numbers from 1 to that number 
def even(num):
    for i in range(1, num+1):
        if i%2==0:
            yield(i)

gen= even(10)
for num in gen:
    print (num)
 
